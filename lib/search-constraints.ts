/** Minimum trimmed length before calling Yahoo suggest/search. */
export const SEARCH_QUERY_MIN_LENGTH = 2;

/** Maximum trimmed length for search and suggest (client + server). */
export const SEARCH_QUERY_MAX_LENGTH = 100;
